-r requirements-audit.txt
pandas==2.3.3
scikit-learn==1.7.2
tensorflow==2.20.0
keras==3.12.0
# Install the CUDA-compatible builds of these packages from the official
# PyTorch index when GPU training is required.
torch==2.10.0
torchvision==0.25.0
